## Automated product-development branch

This pull request is updated by the hourly autonomous-engineering workflow. Each
successful run continues from this branch's existing state, runs the repository
test suite, commits one focused improvement, and pushes it here for review.

Do not merge partial or unreviewed work merely because checks pass. Product and
market claims should remain evidence-based, and repository secrets must never be
included in commits or pull-request output.
